#### ---- Standard Specs for the Direct Coupler ---- ####
                ## All Units in nm ##

λ        = 1550     # nm
H        = 220      # nm (Si layer)
h        = 90       # nm (partial etch)
w        = 500      # nm (waveguide width)
gap_width= 200      # nm (center-to-center spacing between waveguides ~700 nm)
BOX_H    = 2000     # nm
L        = 20e3     # nm = 20 µm (coupling length)
L_3dB = 8.65        # For 3dB Coupling



#### ---- Standard Specs for the MMI Coupler ---- ####

w_wg_MMI = 0.4  # Waveguide width (um).
h_MMI = 0.22  # Silicon layer height (um).
w_MMi = 1.5  # MMI section width (um).
l_MMi = 7.92 # MMI section length (um).
gap_MMi = 0.2  # Gap between output waveguides (um).

# Waveguide lengths.
l_input_MMi = 2.0  # Input waveguide length (um).
l_output_MMi = 3.0  # Output waveguide length (um).

# S-bend parameters.
s_bend_offset_MMi = 1.0  # Lateral offset for S-bends (um).
s_bend_length_MMi = 4.0  # Length of S-bend section (um).

